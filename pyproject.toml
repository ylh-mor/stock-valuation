[project]
name = "stock-val"
version = "0.9.0"
requires-python = "~=3.12.0"
dependencies = [
  "pandas",
  "numpy",
  "scikit-learn",
  "scipy",
  "matplotlib",
  "requests",
  "streamlit",
  "yfinance"
]
